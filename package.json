{
  "name": "caresync-ai",
  "version": "1.0.0",
  "private": true,
  "description": "CARESYNC AI - Connected Healthcare. Smarter Care.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "setup": "cd server && npm install && npx prisma generate && npx prisma db push && npm run seed && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
